
function login() {
      // if (document.getElementById("email").value.length > 0 && document.getElementById("password").value.length > 0) {
      //   window.location.href = "c01-inscrito.html";
      // } else {
      document?.getElementById("alert")?.style.display = "block";
      document?.getElementById("email")?.className += "  border-danger";
      document?.getElementById("senha")?.className += "  border-danger";
      // }
}